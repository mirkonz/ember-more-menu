(function() {
  function vendorModule() {
    'use strict';

    return {
      'default': self['ResizeSensor'],
      __esModule: true,
    };
  }

  define('resize-sensor', [], vendorModule);
})();
